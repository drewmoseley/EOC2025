#include <stdint.h>
#include <stdlib.h>
// #include <stdio.h>


typedef struct {
    uint32_t mode;
  //uint
} GPIO_Config;

int main() {
    GPIO_Config myPin;
    //myPi
}

int main2() {
    int arr[] = {10, 5, 4, 6, 9};
    int n = sizeof(arr) / sizeof(arr[0]);
 
    // qsor
}
